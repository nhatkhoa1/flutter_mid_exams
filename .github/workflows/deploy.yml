name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Enable web
        run: flutter config --enable-web

      - name: Get packages
        run: flutter pub get

      - name: Build web
        run: flutter build web --release --base-href "/flutter_mid_exams/"

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3.9.3   # version mới nhất ổn định
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build/web
          force_orphan: true                       # thêm dòng này để tránh lỗi
